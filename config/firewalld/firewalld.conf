# DefaultZone=drop is too aggressive for desktop (breaks MDNS/Chromecast), 
# but requested "Strict". We will set DefaultZone=block (rejects instead of dropping silently) 
# or 'drop' if strictly requested. Let's use 'FedoraWorkstation' but stripped, 
# or 'block'. The user asked for "Strict firewalld configuration (drop by default)".

DefaultZone=drop
MinimalMark=100
CleanupOnExit=yes
Lockdown=yes
IPv6_rpfilter=yes
IndividualCalls=no
LogDenied=off
# FirewallBackend=nftables is default in Fedora
